func main() {
    let count = 0;
    let n = 1;
    while (n < 10) {
        let num = if ((n % 5) == 0) {
            n
        } else {
            0
        };
        count := count + num;
        n := n + 1;
    };
    count
}